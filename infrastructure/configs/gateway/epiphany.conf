server {
    include /etc/nginx/includes/listen.conf;

    server_name epiphany.workspace.localhost epiphany.workspace.fedora-workstation epiphany.workspace.alexbrasser.com;

    include /etc/nginx/includes/ssl.conf;

    location / {
        include /etc/nginx/includes/proxy.conf;

        resolver ${NGINX_LOCAL_RESOLVERS} valid=5s ipv6=off;
        resolver_timeout 2s;

        set $upstream workspace-epiphany-pod:3000;
        proxy_pass http://$upstream;
    }
}
