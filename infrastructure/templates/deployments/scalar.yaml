{{ if .Values.services.scalar.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-scalar
  labels:
    app: {{ .Release.Name }}-scalar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-scalar
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-scalar
    spec:
      containers:
        - name: scalar
          image: {{ .Values.services.scalar.image }}
          imagePullPolicy: {{ .Values.deployments.containers.pullPolicy }}
          env:
            - name: API_REFERENCE_CONFIG
              value: ""
{{ end }}
